{
  "name": "dsh-catalog-merged",
  "version": "0.1.0",
  "private": true,
  "description": "DSH Market data half — multi-source catalog ingestion, discovery, admission, ranking and the published static site. The settings-section plugin lives in dsh-market-own.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "ingest": "node ingest/index.mjs",
    "ingest:offline": "node ingest/index.mjs --no-discover",
    "db": "node ingest/load-sqlite.mjs --db data/market.sql --apply",
    "test": "node --test",
    "api:dev": "wrangler dev --config api/wrangler.toml",
    "api:deploy": "wrangler deploy --config api/wrangler.toml",
    "check": "node --check ingest/index.mjs && node --check web/app.js && node --check shared/quality.mjs && node --check api/worker.js"
  },
  "engines": { "node": ">=22" },
  "repository": { "type": "git", "url": "https://github.com/zyf-maker/dsh-catalog-merged" }
}
